describe('ArticleService', function() {

	beforeEach(module('newsService'));

	it('should ...', inject(function(dsmArticleService) {

	//expect(dsmArticleService.doSomething()).toEqual('something');

	}));

});
