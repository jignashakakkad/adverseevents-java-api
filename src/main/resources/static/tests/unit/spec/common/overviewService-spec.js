describe('overviewService', function() {

	beforeEach(module('ui.router'));
	beforeEach(module('dataservice'));

	it('should ...', inject(function(overviewService) {

		//expect(overviewService.doSomething()).toEqual('something');

	}));

});