describe('toTrusted', function() {

	beforeEach(module('AdverseEventsExplorer.main'));

	xit('should ...', inject(function($filter) {
        var filter = $filter('toTrusted');
		expect(filter('input')).toEqual('output');
	}));

});