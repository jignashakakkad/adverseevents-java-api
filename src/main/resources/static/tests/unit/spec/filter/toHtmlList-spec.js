describe('toHtmlList', function() {

	beforeEach(module('AdverseEventsExplorer.main'));

	xit('should ...', inject(function($filter) {
        var filter = $filter('toHtmlList');

		expect(filter('input')).toEqual(filter('input'));

	}));

});
